name: deploy-connectivity

on:
  push:
    branches:
      - live

jobs:
  run-tests:
    name: run-tests
    runs-on: [ubuntu-latest]
    steps:
      - uses: actions/checkout@main
      - name: Install dependencies
        run: npm install
      - name: Run unit tests
        run: lerna run deploy --since main
